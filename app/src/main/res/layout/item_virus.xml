<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="10dp" >

    <ImageView
        android:id="@+id/iv_iv_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_launcher" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:descendantFocusability="blocksDescendants" >

        <TextView
            android:id="@+id/tv_iv_name"
            style="@style/NormalText"
            android:text="名字" />

        <TextView
            android:id="@+id/tv_iv_safe"
            style="@style/NormalText"
            android:layout_below="@id/tv_iv_name"
            android:text="安全"
            android:textColor="@color/virus_green"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/iv_iv_clean"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:src="@drawable/clean_seletor" />
    </RelativeLayout>

</LinearLayout>